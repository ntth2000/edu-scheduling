\documentclass[../DoAn.tex]{subfiles}
\begin{document}

\section{Thiết kế kiến trúc}
\subsection{Lựa chọn kiến trúc phần mềm}
\label{subsection:4.1.1}

Mô hình Client--Server được lựa chọn làm kiến trúc tổng thể cho hệ thống
EduSchedule nhằm phân tách rõ ràng giữa thành phần giao tiếp với người dùng và
thành phần xử lý nghiệp vụ, quản lý dữ liệu. Trong mô hình này, Client và Server
được phát triển thành hai ứng dụng độc lập, trao đổi dữ liệu thông qua các API.
Cách tổ chức trên giúp mỗi thành phần có trách nhiệm riêng, thuận tiện cho việc
bảo trì, cập nhật và mở rộng mà không làm thay đổi toàn bộ hệ thống.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{Hinhve/Kientruc/client_server_model.png}
    \caption{Mô hình kiến trúc Client--Server của hệ thống EduSchedule}
    \label{fig:client_server_model}
\end{figure}

Trong mô hình được minh họa tại Hình~\ref{fig:client_server_model}, Client đóng
vai trò là thành phần tương tác trực tiếp với người dùng. Thành phần này được
xây dựng bằng Next.js và TypeScript, có nhiệm vụ hiển thị giao diện, tiếp nhận
thao tác và chuyển các yêu cầu nghiệp vụ đến Server. Các dữ liệu nhận được từ
Server được Client xử lý và trình bày dưới dạng danh sách, biểu mẫu hoặc lưới
thời khóa biểu. Việc tách phần giao diện khỏi xử lý nghiệp vụ giúp giao diện có
thể thay đổi độc lập và hạn chế việc để lộ trực tiếp cơ chế truy cập dữ liệu.

Server đóng vai trò xử lý trung tâm, được xây dựng bằng Java và Spring Boot.
Các Controller tiếp nhận yêu cầu HTTP từ Client, kiểm tra dữ liệu đầu vào và
chuyển yêu cầu đến các Service tương ứng. Tầng Service thực hiện các nghiệp vụ
như quản lý dữ liệu, phân công giảng dạy, kiểm tra xung đột và xếp thời khóa
biểu; tầng Repository đảm nhiệm việc truy cập cơ sở dữ liệu PostgreSQL thông
qua Spring Data JPA. Kết quả xử lý được chuẩn hóa và trả về cho Client dưới
dạng JSON thông qua các RESTful API. Cơ sở dữ liệu chỉ được truy cập từ phía
Server nhằm bảo đảm việc kiểm soát và tính nhất quán của dữ liệu.

Việc áp dụng mô hình Client--Server mang lại một số lợi ích phù hợp với yêu
cầu của EduSchedule. Phần giao diện và phần máy chủ có thể được phát triển,
kiểm thử và triển khai độc lập; các nghiệp vụ và dữ liệu quan trọng được quản lý
tập trung tại Server; đồng thời hệ thống có thể bổ sung giao diện hoặc ứng dụng
khách khác trong tương lai mà vẫn tái sử dụng các API hiện có. Trên cơ sở đó,
đồ án lựa chọn Next.js cho Client, Spring Boot cho Server và PostgreSQL cho
lớp lưu trữ dữ liệu. Cơ chế xác thực bằng JSON Web Token được sử dụng để bảo
vệ các chức năng quản lý, trong khi các API tra cứu thời khóa biểu đã công khai
được cung cấp cho người dùng vãng lai mà không yêu cầu đăng nhập.


\subsection{Thiết kế tổng quan}
\label{subsection:4.1.2}

Hình~\ref{fig:package_diagram} trình bày biểu đồ gói tổng quan của hệ thống
EduSchedule. Hệ thống được chia thành hai thành phần chính là Client và Server;
cơ sở dữ liệu PostgreSQL được đặt độc lập và chỉ được truy cập thông qua phía
Server. Các mũi tên trong biểu đồ thể hiện quan hệ phụ thuộc và hướng trao đổi
giữa các gói.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{Hinhve/Kientruc/overall_system.png}
    \caption{Biểu đồ gói tổng quan của hệ thống EduSchedule}
    \label{fig:package_diagram}
\end{figure}

Phía Client được xây dựng bằng Next.js và gồm ba gói chính. Gói Pages tổ chức
các trang theo chức năng và tiếp nhận thao tác của người dùng. Các trang sử dụng
những thành phần giao diện có khả năng tái sử dụng trong gói Components. Khi
cần truy xuất hoặc cập nhật dữ liệu, Components gọi các hàm trong gói API
Services để gửi yêu cầu đến các RESTful API do Server cung cấp.

Phía Server được xây dựng bằng Spring Boot và tổ chức theo kiến trúc phân
tầng. Gói Controllers tiếp nhận yêu cầu từ API Services, kiểm tra dữ liệu đầu
vào và chuyển yêu cầu đến gói Services. Services thực hiện các nghiệp vụ của hệ
thống như quản lý dữ liệu, phân công giảng dạy, kiểm tra xung đột và xếp thời
khóa biểu. Khi cần đọc hoặc ghi dữ liệu, Services sử dụng các interface trong
gói Repositories; các đối tượng trong gói Entities đảm nhiệm việc ánh xạ dữ liệu
giữa ứng dụng Java và các bảng trong cơ sở dữ liệu.

PostgreSQL nằm ngoài các gói mã nguồn và đóng vai trò lưu trữ dữ liệu lâu dài
cho hệ thống. Việc truy cập cơ sở dữ liệu được thực hiện tập trung thông qua
Repositories, nhờ đó phần xử lý nghiệp vụ không phụ thuộc trực tiếp vào cách
dữ liệu được lưu trữ. Cấu trúc trên tạo ra luồng xử lý thống nhất từ giao diện,
qua các tầng nghiệp vụ đến cơ sở dữ liệu và giúp các thành phần có thể được bảo
trì, kiểm thử hoặc mở rộng tương đối độc lập.


\section{Thiết kế chi tiết}
\subsection{Thiết kế giao diện}
\label{subsection:4.2.1}

\textbf{Thông số màn hình mục tiêu.} Hệ thống EduSchedule là ứng dụng web hướng đến người dùng trên máy tính để bàn và máy tính xách tay. Độ phân giải tối thiểu được hỗ trợ là 1280×768 pixel; độ phân giải khuyến nghị là 1440×900 trở lên. Giao diện xây dựng theo nguyên tắc responsive với TailwindCSS, đảm bảo hiển thị ổn định ở các kích thước màn hình phổ biến từ 1024px trở lên. Hệ thống hỗ trợ hiển thị 16 triệu màu (truecolor) thông qua trình duyệt.

\textbf{Chuẩn hoá giao diện.} Một số quy ước được áp dụng nhất quán trong toàn bộ giao diện:
\begin{itemize}[noitemsep]
    \item \textit{Phối màu}: nền xám nhạt (\#F8F9FA), văn bản đen, màu nhấn xanh dương đậm (\#0037B0) dùng cho sidebar và các thành phần thương hiệu. Ô xung đột trong lưới thời khoá biểu hiển thị nền đỏ nhạt với chữ đỏ đậm; ô chưa lưu dùng tông vàng cam. Riêng cảnh báo giáo viên vượt định mức tiết dạy được hiển thị bằng chữ đỏ trong bảng thống kê ở trang phân công giảng dạy, không hiển thị trực tiếp trên lưới thời khoá biểu.
    \item \textit{Typography}: font hệ thống sans-serif, cỡ chữ cơ sở 14–16px. Tiêu đề section dùng cỡ 18–20px, font-weight 600.
    \item \textit{Nút}: nút mặc định (primary) nền đen/xám đậm, nút phụ (huỷ) nền xám nhạt, nút xoá (destructive) nền đỏ nhạt với chữ đỏ. Tất cả các nút có trạng thái hover và disabled rõ ràng.
    \item \textit{Thông báo phản hồi}: toast notification xuất hiện ở góc trên chính giữa màn hình, thời gian hiển thị thay đổi theo loại thông báo (khoảng 1–6 giây). Biểu tượng xanh lá cho thành công, đỏ cho lỗi.
    \item \textit{Bố cục}: sidebar cố định bên trái rộng 256px chứa menu điều hướng; vùng nội dung chính chiếm phần còn lại.
    \item \textit{Lưới thời khoá biểu}: bảng có hàng là tiết (1–7), cột là thứ (Hai–Sáu). Mỗi ô có kích thước cố định, hiển thị tên môn và tên giáo viên.
\end{itemize}

\subsection{Thiết kế lớp}
\label{subsection:4.2.2}

Phần này trình bày thiết kế chi tiết ba lớp nghiệp vụ cốt lõi của hệ thống — AssignmentService, SlotService và ScheduleGeneratorService — tương ứng với các use case Phân công giáo viên chủ nhiệm/bộ môn, Xếp và điều chỉnh thời khoá biểu thủ công và Xếp thời khoá biểu tự động đã đặc tả ở Chương 2. Use case Đăng nhập và Tra cứu thời khoá biểu không phát sinh lớp nghiệp vụ mới nên không được trình bày lại ở đây.

\textbf{Lớp AssignmentService}

\begin{figure}[H]
    \centering
    % TODO: Chèn biểu đồ lớp chi tiết AssignmentService + Assignment
    \includegraphics[width=0.8\textwidth]{Hinhve/Kientruc/class_assignment.png}
    \caption{Biểu đồ lớp thiết kế chi tiết AssignmentService}
    \label{fig:class_assignment}
\end{figure}

Hình~\ref{fig:class_assignment} trình bày cấu trúc lớp \texttt{AssignmentService}, đóng vai trò then chốt trong việc điều phối nghiệp vụ phân công giảng dạy, bao gồm phân công giáo viên chủ nhiệm và giáo viên bộ môn cho từng lớp–môn. Phương thức \texttt{assignHomeroom()} phân công giáo viên chủ nhiệm cho một lớp: kiểm tra giáo viên thuộc loại CHU\_NHIEM và chưa đảm nhiệm lớp khác trong năm học trước khi cập nhật trường \texttt{homeroomTeacher} của lớp. Phương thức \texttt{assign()} phân công giáo viên bộ môn cho một môn–lớp cụ thể, tạo mới hoặc cập nhật bản ghi \texttt{Assignment} tương ứng. Bên cạnh đó, \texttt{getAll(year)} truy vấn toàn bộ phân công theo năm học và \texttt{delete(id)} xoá một phân công. Các phương thức này tương tác trực tiếp với thực thể \texttt{Assignment} thuộc tầng dữ liệu.

Thực thể \texttt{Assignment} lưu trữ lớp học, môn học, giáo viên phụ trách và số tiết mỗi tuần được phân công, có quan hệ kết hợp nhiều chiều với \texttt{SchoolClass}, \texttt{Subject} và \texttt{Teacher}. Đây là dữ liệu đầu vào quan trọng để \texttt{SlotService} và \texttt{ScheduleGeneratorService} xác định giáo viên phụ trách khi xếp từng tiết học.

\textbf{Lớp SlotService và ScheduleGeneratorService}

\begin{figure}[H]
    \centering
    % TODO: Chèn biểu đồ lớp chi tiết SlotService, ScheduleGeneratorService + Slot
    \includegraphics[width=0.9\textwidth]{Hinhve/Kientruc/class_slot.png}
    \caption{Biểu đồ lớp thiết kế chi tiết SlotService và ScheduleGeneratorService}
    \label{fig:class_slot}
\end{figure}

Hình~\ref{fig:class_slot} mô tả hai lớp cùng thao tác trên thực thể \texttt{Slot} nhưng phục vụ hai use case khác nhau. \texttt{SlotService} chịu trách nhiệm xếp tiết học thủ công: phương thức \texttt{saveOrUpdateSlot()} thực hiện upsert một tiết vào ô (thứ, tiết) cụ thể, trước tiên xác định \texttt{Assignment} tương ứng thông qua \texttt{resolveAssignment()} — nếu yêu cầu chứa \texttt{assignmentId} thì tra cứu trực tiếp, nếu chỉ có \texttt{classId} và \texttt{subjectId} thì tìm hoặc tạo phân công với giáo viên chủ nhiệm của lớp — sau đó kiểm tra xung đột giáo viên qua \texttt{SlotRepository}; nếu giáo viên đã dạy lớp khác cùng buổi và tiết, phương thức ném ngoại lệ và trả lỗi 400, ngược lại tiết được lưu và trả về \texttt{SlotResponse}. Ngoài ra, \texttt{getAll()}/\texttt{getByTimetable()} phục vụ truy vấn danh sách tiết, còn \texttt{deleteSlot()} xoá một tiết theo id.

\texttt{ScheduleGeneratorService} đảm nhiệm nghiệp vụ xếp thời khoá biểu tự động, phân bổ các tiết học còn thiếu bằng thuật toán trong khi giữ nguyên các tiết đã xếp thủ công trước đó. Phương thức \texttt{generate(weekId)} trước tiên xác định số tiết còn thiếu của từng phân công bằng cách so sánh tổng số tiết yêu cầu theo khối (lấy từ \texttt{Subject}) với số tiết đã xếp trong tuần; các phân công còn thiếu tiết và đã có giáo viên phụ trách được đưa vào \texttt{GreedyPhase} để tạo nhanh một phương án khởi tạo đáp ứng các ràng buộc bắt buộc (không trùng giáo viên, không trùng lớp, không vượt số phòng chức năng). Phương án này sau đó được chuyển cho \texttt{TimefoldPhase}, sử dụng \texttt{SolverFactory} của thư viện Timefold Solver, để tiếp tục cải thiện các ràng buộc mềm trong giới hạn thời gian cho phép. Kết quả trả về gồm danh sách tiết đã xếp ở trạng thái chưa lưu cùng danh sách tiết không thể xếp được (nếu có), để người dùng xem xét trước khi quyết định lưu. Khác với \texttt{SlotService} xử lý từng tiết riêng lẻ theo thao tác của người dùng, \texttt{ScheduleGeneratorService} thao tác trên toàn bộ tập tiết còn trống của một tuần cùng lúc, nên được tách thành một lớp nghiệp vụ độc lập.

Thực thể \texttt{Slot} lưu trữ tuần học, phân công tương ứng, thứ, buổi, tiết và phòng chức năng sử dụng (nếu có); hợp thành với \texttt{Timetable} thông qua \texttt{Week} và kết hợp với \texttt{Assignment}.

Các biểu đồ trình tự dưới đây minh hoạ luồng xử lý cho ba use case tương ứng.

\begin{figure}[H]
    \centering
    % TODO: Chèn biểu đồ trình tự UC "Phân công giảng dạy"
    \includegraphics[width=0.95\textwidth]{Hinhve/SequenceDiagram/sd_phancong.png}
    \caption{Biểu đồ trình tự use case Phân công giảng dạy}
    \label{fig:sd_phan_cong}
\end{figure}
\vspace{-10pt}

Trong UC Phân công GVBM: Admin gửi request classId, subjectId, teacherId. AssignmentController gọi AssignmentService.assign(). Service kiểm tra giáo viên có trong danh sách môn dạy được; nếu không ném ngoại lệ. Nếu hợp lệ, tìm hoặc tạo Assignment rồi lưu và trả response.

\begin{figure}[H]
    \centering
    % TODO: Chèn biểu đồ trình tự UC "Xếp tiết học"
    \includegraphics[width=0.95\textwidth]{Hinhve/SequenceDiagram/sd_xeptiet.png}
    \caption{Biểu đồ trình tự use case Xếp tiết học}
    \label{fig:sd_xep_tiet}
\end{figure}
\vspace{-10pt}

Trong UC Xếp tiết học: Admin gửi request chứa timetableId, day, period, assignmentId. SlotController chuyển tiếp sang SlotService.saveOrUpdateSlot(). Service tra cứu Timetable, resolve Assignment, kiểm tra xung đột giáo viên qua SlotRepository. Nếu xung đột, ném ngoại lệ trả về lỗi 400. Nếu không, lưu slot và trả SlotResponse về client.

\begin{figure}[H]
    \centering
    % TODO: Chèn biểu đồ trình tự UC "Xếp thời khoá biểu tự động"
    \includegraphics[width=0.95\textwidth]{Hinhve/SequenceDiagram/sd_xep_tu_dong.png}
    \caption{Biểu đồ trình tự use case Xếp thời khoá biểu tự động}
    \label{fig:sd_xep_tu_dong}
\end{figure}
\vspace{-10pt}

Trong UC Xếp thời khoá biểu tự động: Admin nhấn nút xếp tự động với weekId đang chỉnh sửa. ScheduleGeneratorController chuyển tiếp sang ScheduleGeneratorService.generate(). Service tính số tiết còn thiếu của từng phân công, chạy GreedyPhase để tạo phương án khởi tạo rồi chuyển cho TimefoldPhase tối ưu ràng buộc mềm. Kết quả trả về danh sách tiết đã xếp ở trạng thái chưa lưu để client hiển thị cho người dùng xem xét trước khi lưu.

\subsection{Thiết kế cơ sở dữ liệu}
\label{subsection:4.2.3}

Cơ sở dữ liệu PostgreSQL của hệ thống gồm mười hai bảng, phản ánh trực tiếp từ các entity class trong backend. Thiết kế chi tiết được mô tả trong sơ đồ sau:
\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{Hinhve/database/dbdesign.png}
    \caption{Thiết kế chi tiết cơ sở dữ liệu}
    \label{fig:db_design}
\end{figure}


Từ sơ đồ ER, ta có thể thiết kế chi tiết cấu trúc bảng và ràng buộc cho từng bảng như sau:

\subsubsection*{Bảng users}

\begin{table}[H]
\centering
\small
\caption{Mô tả bảng users}
\label{table:db_users}
\begin{tabular}{|p{3.5cm}|p{3cm}|p{4cm}|p{3cm}|}
\hline
\textbf{Tên cột} & \textbf{Kiểu dữ liệu} & \textbf{Ràng buộc} & \textbf{Mô tả} \\
\hline
id & BIGINT & PK, AUTO\_INCREMENT & Khoá chính \\
\hline
username & VARCHAR(255) & NOT NULL, UNIQUE & Tên đăng nhập \\
\hline
password & VARCHAR(255) & NOT NULL & Mật khẩu (BCrypt hash) \\
\hline
\end{tabular}
\end{table}
\vspace{-8pt}

\subsubsection*{Bảng teachers}

\begin{table}[H]
\centering
\small
\caption{Mô tả bảng teachers}
\label{table:db_teachers}
\begin{tabular}{|p{3.5cm}|p{3cm}|p{4cm}|p{3cm}|}
\hline
\textbf{Tên cột} & \textbf{Kiểu dữ liệu} & \textbf{Ràng buộc} & \textbf{Mô tả} \\
\hline
id & BIGINT & PK, AUTO\_INCREMENT & Khoá chính \\
\hline
full\_name & VARCHAR(255) & NOT NULL & Họ và tên giáo viên \\
\hline
user\_id & BIGINT & NOT NULL, FK $\rightarrow$ users & Tài khoản sở hữu \\
\hline
type & VARCHAR(20) & NOT NULL & Loại: BO\_MON, CHU\_NHIEM, KHAC \\
\hline
max\_periods\_per\_week & INTEGER & NOT NULL & Số tiết tối đa mỗi tuần \\
\hline
is\_active & BOOLEAN & NOT NULL, DEFAULT true & Trạng thái hoạt động \\
\hline
created\_at & TIMESTAMP & NOT NULL & Thời điểm tạo bản ghi \\
\hline
\end{tabular}
\end{table}
\vspace{-8pt}

\subsubsection*{Bảng subjects}

\begin{table}[H]
\centering
\small
\caption{Mô tả bảng subjects}
\label{table:db_subjects}
\begin{tabular}{|p{3.5cm}|p{3cm}|p{4cm}|p{3cm}|}
\hline
\textbf{Tên cột} & \textbf{Kiểu dữ liệu} & \textbf{Ràng buộc} & \textbf{Mô tả} \\
\hline
id & BIGINT & PK, AUTO\_INCREMENT & Khoá chính \\
\hline
user\_id & BIGINT & NOT NULL, FK $\rightarrow$ users & Tài khoản sở hữu \\
\hline
name & VARCHAR(100) & NOT NULL & Tên môn học \\
\hline
periods\_grade1 & INTEGER & NOT NULL & Số tiết/tuần khối 1 \\
\hline
periods\_grade2 & INTEGER & NOT NULL & Số tiết/tuần khối 2 \\
\hline
periods\_grade3 & INTEGER & NOT NULL & Số tiết/tuần khối 3 \\
\hline
periods\_grade4 & INTEGER & NOT NULL & Số tiết/tuần khối 4 \\
\hline
periods\_grade5 & INTEGER & NOT NULL & Số tiết/tuần khối 5 \\
\hline
\end{tabular}
\end{table}
\vspace{-8pt}

\subsubsection*{Bảng teacher\_subjects}

\begin{table}[H]
\centering
\small
\caption{Mô tả bảng teacher\_subjects}
\label{table:db_teacher_subjects}
\begin{tabular}{|p{3.5cm}|p{3cm}|p{4cm}|p{3cm}|}
\hline
\textbf{Tên cột} & \textbf{Kiểu dữ liệu} & \textbf{Ràng buộc} & \textbf{Mô tả} \\
\hline
teacher\_id & BIGINT & PK, FK $\rightarrow$ teachers & Khoá ngoại giáo viên \\
\hline
subject\_id & BIGINT & PK, FK $\rightarrow$ subjects & Khoá ngoại môn học \\
\hline
\end{tabular}
\end{table}
\vspace{-8pt}

Bảng trung gian thể hiện quan hệ nhiều–nhiều giữa giáo viên và các môn học họ có thể dạy.

\subsubsection*{Bảng school\_years}

\begin{table}[H]
\centering
\small
\caption{Mô tả bảng school\_years}
\label{table:db_school_years}
\begin{tabular}{|p{3.5cm}|p{3cm}|p{4cm}|p{3cm}|}
\hline
\textbf{Tên cột} & \textbf{Kiểu dữ liệu} & \textbf{Ràng buộc} & \textbf{Mô tả} \\
\hline
id & BIGINT & PK, AUTO\_INCREMENT & Khoá chính \\
\hline
user\_id & BIGINT & NOT NULL, FK $\rightarrow$ users & Tài khoản sở hữu \\
\hline
start\_year & INTEGER & NOT NULL & Năm bắt đầu (VD: 2024) \\
\hline
\multicolumn{4}{|l|}{\textit{UNIQUE(user\_id, start\_year)}} \\
\hline
\end{tabular}
\end{table}
\vspace{-8pt}

\subsubsection*{Bảng special\_rooms}

\begin{table}[H]
\centering
\small
\caption{Mô tả bảng special\_rooms}
\label{table:db_special_rooms}
\begin{tabular}{|p{3.5cm}|p{3cm}|p{4cm}|p{3cm}|}
\hline
\textbf{Tên cột} & \textbf{Kiểu dữ liệu} & \textbf{Ràng buộc} & \textbf{Mô tả} \\
\hline
id & BIGINT & PK, AUTO\_INCREMENT & Khoá chính \\
\hline
user\_id & BIGINT & NOT NULL, FK $\rightarrow$ users & Tài khoản sở hữu \\
\hline
name & VARCHAR(100) & NOT NULL & Tên phòng chức năng \\
\hline
subject\_id & BIGINT & FK $\rightarrow$ subjects, NULL & Môn học sử dụng phòng này \\
\hline
quantity & INTEGER & NOT NULL, DEFAULT 1 & Số lượng phòng hiện có \\
\hline
\end{tabular}
\end{table}
\vspace{-8pt}

\subsubsection*{Bảng classes}

\begin{table}[H]
\centering
\small
\caption{Mô tả bảng classes}
\label{table:db_classes}
\begin{tabular}{|p{3.5cm}|p{3cm}|p{4cm}|p{3cm}|}
\hline
\textbf{Tên cột} & \textbf{Kiểu dữ liệu} & \textbf{Ràng buộc} & \textbf{Mô tả} \\
\hline
id & BIGINT & PK, AUTO\_INCREMENT & Khoá chính \\
\hline
name & VARCHAR(10) & NOT NULL & Tên lớp (VD: 1A, 3B) \\
\hline
grade & INTEGER & NOT NULL & Khối lớp (1–5) \\
\hline
homeroom\_teacher\_id & BIGINT & FK $\rightarrow$ teachers, NULL & Giáo viên chủ nhiệm \\
\hline
school\_year\_id & BIGINT & FK $\rightarrow$ school\_years, NULL & Năm học \\
\hline
\multicolumn{4}{|l|}{\textit{UNIQUE(name, school\_year\_id)}} \\
\hline
\end{tabular}
\end{table}
\vspace{-8pt}

\subsubsection*{Bảng assignments}

\begin{table}[H]
\centering
\small
\caption{Mô tả bảng assignments}
\label{table:db_assignments}
\begin{tabular}{|p{3.5cm}|p{3cm}|p{4cm}|p{3cm}|}
\hline
\textbf{Tên cột} & \textbf{Kiểu dữ liệu} & \textbf{Ràng buộc} & \textbf{Mô tả} \\
\hline
id & BIGINT & PK, AUTO\_INCREMENT & Khoá chính \\
\hline
class\_id & BIGINT & NOT NULL, FK $\rightarrow$ classes & Lớp học được phân công \\
\hline
subject\_id & BIGINT & NOT NULL, FK $\rightarrow$ subjects & Môn học được phân công \\
\hline
teacher\_id & BIGINT & FK $\rightarrow$ teachers, NULL & Giáo viên được phân công \\
\hline
periods\_per\_week & INTEGER & NOT NULL, DEFAULT 0 & Số tiết/tuần được phân công \\
\hline
\multicolumn{4}{|l|}{\textit{UNIQUE(class\_id, subject\_id, teacher\_id)}} \\
\hline
\end{tabular}
\end{table}
\vspace{-8pt}

\subsubsection*{Bảng timetables}

\begin{table}[H]
\centering
\small
\caption{Mô tả bảng timetables}
\label{table:db_timetables}
\begin{tabular}{|p{3.5cm}|p{3cm}|p{4cm}|p{3cm}|}
\hline
\textbf{Tên cột} & \textbf{Kiểu dữ liệu} & \textbf{Ràng buộc} & \textbf{Mô tả} \\
\hline
id & BIGINT & PK, AUTO\_INCREMENT & Khoá chính \\
\hline
school\_year\_id & BIGINT & NOT NULL, FK $\rightarrow$ school\_years & Năm học \\
\hline
semester\_order & INTEGER & NOT NULL & Thứ tự học kì (1 hoặc 2) \\
\hline
\multicolumn{4}{|l|}{\textit{UNIQUE(school\_year\_id, semester\_order)}} \\
\hline
\end{tabular}
\end{table}
\vspace{-8pt}

\subsubsection*{Bảng weeks}

\begin{table}[H]
\centering
\small
\caption{Mô tả bảng weeks}
\label{table:db_weeks}
\begin{tabular}{|p{3.5cm}|p{3cm}|p{4cm}|p{3cm}|}
\hline
\textbf{Tên cột} & \textbf{Kiểu dữ liệu} & \textbf{Ràng buộc} & \textbf{Mô tả} \\
\hline
id & BIGINT & PK, AUTO\_INCREMENT & Khoá chính \\
\hline
timetable\_id & BIGINT & NOT NULL, FK $\rightarrow$ timetables & Thời khoá biểu chứa tuần này \\
\hline
week\_number & INTEGER & NOT NULL & Số thứ tự tuần trong học kì \\
\hline
start\_date & DATE & NULL & Ngày bắt đầu của tuần \\
\hline
\multicolumn{4}{|l|}{\textit{UNIQUE(timetable\_id, week\_number)}} \\
\hline
\end{tabular}
\end{table}
\vspace{-8pt}

\subsubsection*{Bảng slots}

\begin{table}[H]
\centering
\small
\caption{Mô tả bảng slots}
\label{table:db_slots}
\begin{tabular}{|p{3.5cm}|p{3cm}|p{4cm}|p{3cm}|}
\hline
\textbf{Tên cột} & \textbf{Kiểu dữ liệu} & \textbf{Ràng buộc} & \textbf{Mô tả} \\
\hline
id & BIGINT & PK, AUTO\_INCREMENT & Khoá chính \\
\hline
week\_id & BIGINT & NOT NULL, FK $\rightarrow$ weeks & Tuần học chứa tiết này \\
\hline
assignment\_id & BIGINT & FK $\rightarrow$ assignments, NULL & Phân công tương ứng \\
\hline
day & INTEGER & NOT NULL & Thứ trong tuần (2–6) \\
\hline
session & INTEGER & NOT NULL & Buổi học (1: sáng, 2: chiều) \\
\hline
period & INTEGER & NOT NULL & Tiết trong buổi (1–4 sáng, 1–3 chiều) \\
\hline
special\_room\_id & BIGINT & FK $\rightarrow$ special\_rooms, NULL & Phòng chức năng sử dụng \\
\hline
\multicolumn{4}{|l|}{\textit{UNIQUE(week\_id, assignment\_id, day, session, period)}} \\
\hline
\end{tabular}
\end{table}
\vspace{-8pt}

\section{Xây dựng ứng dụng}
\subsection{Thư viện và công cụ sử dụng}
\label{subsection:4.3.1}

 Bảng~\ref{table:tools} liệt kê các thư viện và công cụ chính được sử dụng
trong quá trình xây dựng hệ thống EduSchedule. Các công nghệ được phân chia
thành ba nhóm gồm phía máy chủ, phía giao diện, cùng các công cụ hỗ trợ phát
triển và triển khai.

{\small
\begin{longtable}{|L{5.7cm}|L{5.6cm}|L{2.2cm}|}
\caption{Danh sách thư viện và công cụ sử dụng}
\label{table:tools} \\
\hline
\textbf{Mục đích} & \textbf{Công cụ hoặc thư viện} & \textbf{Phiên bản} \\
\hline
\endfirsthead
\hline
\textbf{Mục đích} & \textbf{Công cụ hoặc thư viện} & \textbf{Phiên bản} \\
\hline
\endhead
\multicolumn{3}{|l|}{\textit{Phía máy chủ}} \\
\hline
Ngôn ngữ lập trình & Java & 21 \\
\hline
Khung phát triển phía máy chủ & Spring Boot & 4.0.5 \\
\hline
Bảo mật và phân quyền & Spring Security & Đi kèm Spring Boot \\
\hline
Truy cập và ánh xạ dữ liệu & Spring Data JPA & Đi kèm Spring Boot \\
\hline
Tạo và xác thực mã JWT & JJWT & 0.13.0 \\
\hline
Quản lý thay đổi cấu trúc cơ sở dữ liệu & Flyway & Đi kèm Spring Boot \\
\hline
Cơ sở dữ liệu quan hệ & PostgreSQL & 17 \\
\hline
Tự động tạo mã nguồn hỗ trợ & Lombok & Đi kèm Spring Boot \\
\hline
Xếp thời khoá biểu tự động & Timefold Solver & 2.2.0 \\
\hline
Kiểm thử phía máy chủ & JUnit, Spring Boot Test và MockMvc & Đi kèm Spring Boot \\
\hline
\multicolumn{3}{|l|}{\textit{Phía giao diện}} \\
\hline
Khung phát triển giao diện & Next.js & 16.2.1 \\
\hline
Thư viện xây dựng giao diện & React & 19.2.4 \\
\hline
Ngôn ngữ lập trình & TypeScript & 5 \\
\hline
Thiết kế giao diện bằng CSS & Tailwind CSS & 4 \\
\hline
Thư viện thành phần giao diện & shadcn/ui & 4.1.0 \\
\hline
Kiểm tra dữ liệu biểu mẫu & Zod & 4.3.6 \\
\hline
Xuất tệp Excel & SheetJS (xlsx) & 0.18.5 \\
\hline
Hiển thị thông báo phản hồi & Sonner & 2.0.7 \\
\hline
Kiểm thử thành phần giao diện & Vitest và React Testing Library & 4.1.8 và 16.3.2 \\
\hline
\multicolumn{3}{|l|}{\textit{Công cụ phát triển và triển khai}} \\
\hline
Môi trường phát triển phía máy chủ & IntelliJ IDEA & 2024.x \\
\hline
Môi trường phát triển phía giao diện & Visual Studio Code & 1.9x \\
\hline
Kiểm thử API & Postman & 11.x \\
\hline
Đóng gói ứng dụng & Docker & 27.x \\
\hline
Triển khai phía giao diện & Vercel & -- \\
\hline
Triển khai phía máy chủ & Render & -- \\
\hline
Quản lý mã nguồn & Git và GitHub & -- \\
\hline
\end{longtable}
}
\vspace{-8pt}

\subsection{Kết quả đạt được và minh họa các chức năng chính}
\label{subsection:4.3.2}

Sản phẩm EduSchedule gồm hai thành phần độc lập được triển khai riêng biệt:
\begin{itemize}[noitemsep]
    \item \textbf{eduschedule-backend}: ứng dụng Spring Boot đóng gói thành Docker image, cung cấp RESTful API.
    \item \textbf{eduschedule-frontend}: ứng dụng Next.js triển khai trên Vercel, phục vụ giao diện web.
\end{itemize}

Hệ thống hiện thực đầy đủ các chức năng đã đặt ra trong phần phân tích yêu cầu: quản lý danh mục giáo viên, lớp học, môn học; phân công giảng dạy; xếp thời khoá biểu với kiểm tra xung đột thời gian thực; xem và xuất thời khoá biểu ra file Excel.

Các hình dưới đây minh họa một số chức năng chính đã được hiện thực trong hệ
thống, bao gồm giao diện tổng quan, giao diện quản lý giáo viên và giao diện xếp
thời khóa biểu.

\begin{figure}[H]
    \centering
    \begin{minipage}{0.95\textwidth}
        \centering
        \includegraphics[width=\linewidth]{Hinhve/GiaoDien/giao_dien_tong_quan.png}
        \caption{Giao diện tổng quan hệ thống EduSchedule}
        \label{fig:ui_overview}
    \end{minipage}

    \vspace{0.5cm}

    \begin{minipage}{0.95\textwidth}
        \centering
        \includegraphics[width=\linewidth]{Hinhve/GiaoDien/giao_dien_giao_vien.png}
        \caption{Giao diện quản lý giáo viên của hệ thống EduSchedule}
        \label{fig:ui_teacher}
    \end{minipage}

    \vspace{0.5cm}

    \begin{minipage}{0.95\textwidth}
        \centering
        \includegraphics[width=\linewidth]{Hinhve/GiaoDien/giao_dien_xep_tkb.png}
        \caption{Giao diện xếp thời khóa biểu của hệ thống EduSchedule}
        \label{fig:ui_scheduler}
    \end{minipage}
\end{figure}

\section{Kiểm thử}
\label{section:4.4}

Quá trình kiểm thử được xây dựng dựa trên các ca sử dụng đã phân tích trong
Chương~2, tập trung kiểm tra luồng chính, các trường hợp dữ liệu không hợp lệ
và những ràng buộc nghiệp vụ quan trọng của hệ thống. Các ca kiểm thử API
backend được thực hiện bằng JUnit, Spring Boot Test và MockMvc; các thành phần
giao diện được kiểm tra bằng Vitest, React Testing Library và kiểm thử thủ công
trên trình duyệt Chrome. Mỗi mục dưới đây tương ứng với một ca sử dụng, trong
đó bảng kiểm thử trình bày điều kiện hoặc dữ liệu đầu vào, thao tác thực hiện và
kết quả mong đợi.

\newcommand{\testcaseTableHeader}{%
\hline
\textbf{Mã kiểm thử} & \textbf{Điều kiện hoặc dữ liệu kiểm thử} &
\textbf{Thao tác} & \textbf{Kết quả mong đợi} \\
\hline
\endfirsthead
\hline
\textbf{Mã kiểm thử} & \textbf{Điều kiện hoặc dữ liệu kiểm thử} &
\textbf{Thao tác} & \textbf{Kết quả mong đợi} \\
\hline
\endhead
}

\Needspace{12\baselineskip}
\subsection{Kiểm thử ca sử dụng Đăng nhập}
\label{subsection:test_uc01}

Bảng~\ref{table:test_uc01} trình bày các trường hợp kiểm thử cho ca sử dụng
UC01 - Đăng nhập.

{\small
\begin{longtable}{|L{1.7cm}|L{3.2cm}|L{3cm}|L{4.9cm}|}
\caption{Kiểm thử ca sử dụng Đăng nhập}
\label{table:test_uc01} \\
\testcaseTableHeader
TC01-01 & Tên đăng nhập và mật khẩu hợp lệ. &
Nhập thông tin và chọn ``Đăng nhập''. &
Đăng nhập thành công và chuyển người dùng đến trang chính. \\
\hline
TC01-02 & Tên đăng nhập hợp lệ, mật khẩu không đúng. &
Nhập thông tin và chọn ``Đăng nhập''. &
Hệ thống từ chối đăng nhập, hiển thị thông báo tên đăng nhập hoặc mật khẩu không chính xác. \\
\hline
TC01-03 & Bỏ trống tên đăng nhập hoặc mật khẩu. &
Chọn ``Đăng nhập''. &
Hệ thống cảnh báo trường bắt buộc và không gửi yêu cầu đăng nhập. \\
\hline
TC01-04 & Không có token xác thực hợp lệ. &
Truy cập trực tiếp một trang quản lý. &
Hệ thống không cho phép truy cập và chuyển người dùng về trang đăng nhập. \\
\hline
\end{longtable}
}

\Needspace{12\baselineskip}
\subsection{Kiểm thử ca sử dụng Phân công giáo viên chủ nhiệm}
\label{subsection:test_uc02}

Bảng~\ref{table:test_uc02} trình bày các trường hợp kiểm thử cho ca sử dụng
UC02 - Phân công giáo viên chủ nhiệm.

{\small
\begin{longtable}{|L{1.7cm}|L{3.2cm}|L{3cm}|L{4.9cm}|}
\caption{Kiểm thử ca sử dụng Phân công giáo viên chủ nhiệm}
\label{table:test_uc02} \\
\testcaseTableHeader
TC02-01 & Lớp chưa có giáo viên chủ nhiệm; giáo viên thuộc loại chủ nhiệm và chưa chủ nhiệm lớp khác. &
Chọn lớp, chọn giáo viên và lưu phân công. &
Giáo viên được gán làm chủ nhiệm của lớp và dữ liệu được cập nhật trên giao diện. \\
\hline
TC02-02 & Lớp đã có giáo viên chủ nhiệm. &
Chọn một giáo viên chủ nhiệm hợp lệ khác và lưu. &
Phân công cũ được thay thế bằng giáo viên mới. \\
\hline
TC02-03 & Giáo viên đã là chủ nhiệm của một lớp khác trong cùng năm học. &
Chọn giáo viên đó cho lớp mới. &
Giáo viên không xuất hiện trong danh sách có thể chọn hoặc hệ thống từ chối lưu và thông báo nguyên nhân. \\
\hline
TC02-04 & Lớp đang có giáo viên chủ nhiệm. &
Chọn giá trị trống và xác nhận xoá phân công. &
Giáo viên chủ nhiệm được gỡ khỏi lớp và giao diện được cập nhật. \\
\hline
\end{longtable}
}

\Needspace{12\baselineskip}
\subsection{Kiểm thử ca sử dụng Phân công giáo viên bộ môn}
\label{subsection:test_uc03}

Bảng~\ref{table:test_uc03} trình bày các trường hợp kiểm thử cho ca sử dụng
UC03 - Phân công giáo viên bộ môn.

{\small
\begin{longtable}{|L{1.7cm}|L{3.2cm}|L{3cm}|L{4.9cm}|}
\caption{Kiểm thử ca sử dụng Phân công giáo viên bộ môn}
\label{table:test_uc03} \\
\testcaseTableHeader
TC03-01 & Lớp, môn học và giáo viên hợp lệ; môn học của lớp chưa được phân công. &
Chọn giáo viên tương ứng với môn học của lớp và lưu. &
Phân công được lưu; tổng số tiết đã phân công của giáo viên được cập nhật. \\
\hline
TC03-02 & Môn học của lớp đã được phân công cho một giáo viên khác. &
Chọn giáo viên mới và lưu. &
Hệ thống xoá phân công cũ và lưu phân công cho giáo viên mới. \\
\hline
TC03-03 & Có dữ liệu phân công giảng dạy. &
Chuyển giữa chế độ xem theo giáo viên và theo lớp. &
Hệ thống hiển thị cùng dữ liệu phân công theo đúng đối tượng được lựa chọn. \\
\hline
TC03-04 & Tổng số tiết sau khi phân công vượt định mức của giáo viên. &
Thực hiện và lưu phân công. &
Hệ thống vẫn lưu dữ liệu và hiển thị số tiết vượt định mức bằng trạng thái cảnh báo. \\
\hline
\end{longtable}
}

\Needspace{12\baselineskip}
\subsection{Kiểm thử ca sử dụng Xếp và điều chỉnh thời khoá biểu thủ công}
\label{subsection:test_uc04}

Bảng~\ref{table:test_uc04} trình bày các trường hợp kiểm thử cho ca sử dụng
UC04 - Xếp và điều chỉnh thời khoá biểu thủ công.

{\small
\begin{longtable}{|L{1.7cm}|L{3.2cm}|L{3cm}|L{4.9cm}|}
\caption{Kiểm thử ca sử dụng Xếp và điều chỉnh thời khoá biểu thủ công}
\label{table:test_uc04} \\
\testcaseTableHeader
TC04-01 & Tuần, lớp, môn học và phân công hợp lệ; ô thời khoá biểu đang trống. &
Chọn ô theo thứ, buổi và tiết; chọn môn học cần xếp. &
Tiết học được lưu vào đúng ô và lưới thời khoá biểu được cập nhật. \\
\hline
TC04-02 & Ô đã có một tiết học. &
Chọn môn học khác để thay thế nội dung của ô. &
Hệ thống kiểm tra lại các ràng buộc và cập nhật tiết học khi dữ liệu hợp lệ. \\
\hline
TC04-03 & Giáo viên đang dạy một lớp khác tại cùng thứ, buổi và tiết. &
Xếp môn học do giáo viên đó phụ trách vào ô. &
Hệ thống hiển thị xung đột giáo viên và không tạo lịch trùng ngoài lựa chọn xử lý của người dùng. \\
\hline
TC04-04 & Môn học chưa được phân công giáo viên cho lớp. &
Chọn môn học để xếp vào ô. &
Hệ thống thông báo cần hoàn tất phân công và không lưu tiết học. \\
\hline
TC04-05 & Môn học đã được xếp đủ số tiết quy định trong tuần. &
Mở danh sách môn học tại một ô trống trên lưới thời khoá biểu. &
Môn học đã đủ số tiết không còn khả dụng để lựa chọn, do đó người dùng không thể xếp thêm tiết cho môn học này. \\
\hline
TC04-06 & Môn học cần phòng chức năng nhưng không còn phòng trống tại khung giờ đã chọn. &
Xếp môn học vào ô tương ứng. &
Hệ thống thông báo xung đột phòng chức năng và không lưu tiết học. \\
\hline
TC04-07 & Thời khoá biểu đã có tiết Toán của một lớp; giáo viên phụ trách môn Toán của lớp được thay đổi và giáo viên mới đang dạy lớp khác tại cùng thời điểm. &
Mở lại thời khoá biểu sau khi thay đổi phân công giảng dạy. &
Hệ thống kiểm tra tiết đã xếp theo phân công hiện tại, làm nổi bật các ô bị xung đột và hiển thị thông báo nêu rõ giáo viên, lớp học và thời điểm xảy ra xung đột. \\
\hline
\end{longtable}
}

\Needspace{12\baselineskip}
\subsection{Kiểm thử ca sử dụng Xếp thời khoá biểu tự động}
\label{subsection:test_uc05}

Bảng~\ref{table:test_uc05} trình bày các trường hợp kiểm thử cho ca sử dụng
UC05 - Xếp thời khoá biểu tự động.

{\small
\begin{longtable}{|L{1.7cm}|L{3.2cm}|L{3cm}|L{4.9cm}|}
\caption{Kiểm thử ca sử dụng Xếp thời khoá biểu tự động}
\label{table:test_uc05} \\
\testcaseTableHeader
TC05-01 & Các lớp và môn học đã được phân công đầy đủ; dữ liệu có phương án xếp khả thi. &
Chọn tuần và thực hiện ``Tự động xếp TKB''. &
Hệ thống tạo đủ các tiết cần thiết, không trùng lớp, giáo viên hoặc phòng chức năng. \\
\hline
TC05-02 & Tuần đã có một số tiết được xếp thủ công. &
Thực hiện xếp tự động. &
Hệ thống giữ nguyên các tiết đã có và chỉ tạo phương án cho những tiết còn thiếu. \\
\hline
TC05-03 & Còn lớp hoặc môn học chưa được phân công giáo viên. &
Thực hiện xếp tự động. &
Hệ thống liệt kê dữ liệu chưa hoàn tất và không gửi yêu cầu xếp lịch. \\
\hline
TC05-04 & Không tồn tại phương án đáp ứng toàn bộ yêu cầu xếp lịch. &
Thực hiện xếp tự động. &
Hệ thống hiển thị các tiết xếp được và danh sách tiết chưa thể xếp để người dùng tiếp tục xử lý thủ công. \\
\hline
TC05-05 & Kết quả tự động đang ở trạng thái chưa lưu. &
Chọn lưu tuần hiện tại hoặc áp dụng từ tuần hiện tại trở đi. &
Hệ thống lưu kết quả theo đúng phạm vi người dùng lựa chọn. \\
\hline
TC05-06 & Kết quả tự động đang ở trạng thái chưa lưu. &
Đóng màn hình hoặc chuyển tuần rồi xác nhận huỷ thay đổi. &
Kết quả chưa lưu bị loại bỏ và thời khoá biểu trở về trạng thái trước khi xếp tự động. \\
\hline
\end{longtable}
}

\Needspace{12\baselineskip}
\subsection{Kiểm thử ca sử dụng Tra cứu thời khoá biểu}
\label{subsection:test_uc06}

Bảng~\ref{table:test_uc06} trình bày các trường hợp kiểm thử cho ca sử dụng
UC06 - Tra cứu thời khoá biểu.

{\small
\begin{longtable}{|L{1.7cm}|L{3.2cm}|L{3cm}|L{4.9cm}|}
\caption{Kiểm thử ca sử dụng Tra cứu thời khoá biểu}
\label{table:test_uc06} \\
\testcaseTableHeader
TC06-01 & Có thời khoá biểu đã được công khai. &
Người dùng vãng lai chọn năm học, học kì, tuần và một lớp cần tra cứu. &
Hệ thống hiển thị đúng thời khoá biểu của lớp dưới dạng lưới mà không yêu cầu đăng nhập. \\
\hline
TC06-02 & Có dữ liệu thời khoá biểu theo giáo viên hoặc theo khối. &
Thay đổi chế độ xem và chọn đối tượng cần tra cứu. &
Lưới thời khoá biểu được cập nhật theo đúng chế độ xem và đối tượng đã chọn. \\
\hline
TC06-03 & Người dùng chưa chọn lớp, khối hoặc giáo viên. &
Yêu cầu hiển thị thời khoá biểu. &
Hệ thống yêu cầu lựa chọn đối tượng cần tra cứu. \\
\hline
TC06-04 & Không có thời khoá biểu công khai phù hợp với tiêu chí. &
Chọn các tiêu chí tra cứu tương ứng. &
Hệ thống hiển thị thông báo chưa có dữ liệu và không hiển thị dữ liệu chưa được công khai. \\
\hline
\end{longtable}
}

\Needspace{12\baselineskip}
\subsection{Kiểm thử ca sử dụng Công khai thời khoá biểu}
\label{subsection:test_uc13}

Bảng~\ref{table:test_uc13} trình bày các trường hợp kiểm thử cho ca sử dụng
UC13 - Công khai thời khoá biểu.

{\small
\begin{longtable}{|L{1.7cm}|L{3.2cm}|L{3cm}|L{4.9cm}|}
\caption{Kiểm thử ca sử dụng Công khai thời khoá biểu}
\label{table:test_uc13} \\
\testcaseTableHeader
TC13-01 & Thời khoá biểu đã có dữ liệu và chưa được công khai. &
Chọn ``Công khai thời khoá biểu'' và xác nhận. &
Trạng thái thời khoá biểu được cập nhật; người dùng vãng lai có thể tra cứu dữ liệu. \\
\hline
TC13-02 & Hộp thoại xác nhận công khai đang hiển thị. &
Chọn huỷ thao tác. &
Trạng thái thời khoá biểu không thay đổi. \\
\hline
TC13-03 & Thời khoá biểu chưa có tiết học. &
Thực hiện công khai. &
Hệ thống thông báo không thể công khai thời khoá biểu chưa có dữ liệu. \\
\hline
\end{longtable}
}

\Needspace{12\baselineskip}
\subsection{Kiểm thử ca sử dụng Xuất thời khoá biểu}
\label{subsection:test_uc14}

Bảng~\ref{table:test_uc14} trình bày các trường hợp kiểm thử cho ca sử dụng
UC14 - Xuất thời khoá biểu.

{\small
\begin{longtable}{|L{1.7cm}|L{3.2cm}|L{3cm}|L{4.9cm}|}
\caption{Kiểm thử ca sử dụng Xuất thời khoá biểu}
\label{table:test_uc14} \\
\testcaseTableHeader
TC14-01 & Thời khoá biểu có dữ liệu; đã chọn năm học, học kì, tuần và lớp. &
Chọn chế độ xem theo lớp và chọn ``Xuất Excel''. &
Hệ thống tải xuống tệp \texttt{.xlsx} chứa đúng thời khoá biểu của lớp đã chọn. \\
\hline
TC14-02 & Thời khoá biểu có dữ liệu theo khối hoặc theo giáo viên. &
Chọn chế độ xem, chọn đối tượng tương ứng và xuất tệp. &
Tệp Excel được tạo với đúng dữ liệu và tiêu đề theo chế độ xem đã chọn. \\
\hline
TC14-03 & Không có dữ liệu phù hợp với tiêu chí đã chọn. &
Chọn ``Xuất Excel''. &
Hệ thống thông báo chưa có dữ liệu để xuất và không tạo tệp rỗng. \\
\hline
\end{longtable}
}

\section{Triển khai}
\label{section:4.5}

Hệ thống EduSchedule được triển khai thử nghiệm theo mô hình cloud-based miễn phí, tách biệt hoàn toàn giữa frontend và backend.

\textbf{Frontend — Vercel.} Ứng dụng Next.js được triển khai trực tiếp từ repository GitHub lên Vercel. Mỗi lần push code lên nhánh main, Vercel tự động chạy build và deploy phiên bản mới (CI/CD). Vercel cung cấp HTTPS tự động qua TLS, CDN toàn cầu, và đảm bảo tỷ lệ uptime cao theo SLA của nền tảng. URL truy cập frontend được Vercel cấp theo dạng *.vercel.app.

\textbf{Backend — Docker + Render.} Ứng dụng Spring Boot được đóng gói thành Docker image thông qua Dockerfile. Image chạy trên cổng 8080 bên trong container. Biến môi trường nhạy cảm (chuỗi kết nối database, JWT secret) được truyền vào container qua biến môi trường, không hardcode trong image. Backend được triển khai lên Render — nền tảng cloud hỗ trợ chạy Docker container miễn phí. Render tự động pull image từ Docker Hub, khởi chạy container và cấp URL dạng \textit{*.onrender.com}. Ở gói miễn phí, container tự tắt sau 15 phút không có request và khởi động lại khi có request tiếp theo (cold start).

\textbf{Cơ sở dữ liệu — PostgreSQL.} Database PostgreSQL được cung cấp bởi dịch vụ Render PostgreSQL (gói miễn phí). Schema được Flyway tự động khởi tạo và migrate khi backend khởi động lần đầu, đảm bảo môi trường production đồng bộ với development.

Kết chương: Chương này đã trình bày toàn bộ quá trình thiết kế và xây dựng hệ thống EduSchedule, từ kiến trúc phần mềm, thiết kế chi tiết các lớp và cơ sở dữ liệu, đến kết quả cài đặt, kiểm thử và triển khai. Chương 5 tiếp theo sẽ trình bày các giải pháp và đóng góp nổi bật của đề tài.


\end{document}
