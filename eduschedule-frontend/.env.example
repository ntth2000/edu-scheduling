# Backend API
NEXT_PUBLIC_API_URL=<YOUR BACKEND API URL>

# This frontend app's own public origin (used to build shareable links, e.g.
# the public timetable URL). Falls back to the browser's origin if unset.
NEXT_PUBLIC_APP_URL=<YOUR FRONTEND APP URL>